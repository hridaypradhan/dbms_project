name: dbms_project
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  date_time_picker: ^2.0.0
  expand_widget: ^2.0.0
  ff_navigation_bar: ^0.1.5
  flutter_launcher_icons: ^0.9.0
  provider: ^4.0.2
  simple_coverflow: ^0.0.8
  sqflite: ^2.0.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/projectLogo.png"


flutter:

 
  uses-material-design: true
  assets:
    - assets/
 